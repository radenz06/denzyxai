# denzyx AI — tema warna (opsional, hapus/rename file ini utk default)
# Format tiap baris:  nama = fg,bg
#   fg/bg  : indeks warna 0-255 (-1 = warna default terminal)
# Hanya nama di daftar bawah yang dipakai; baris rusak / tanpa nama
# dikenal akan diabaikan (pakai default). Edit lalu restart app.

# banner      = 51,-1        # highlight/banner/judul/footer (cyan)
# user        = 46,-1        # pesan KAMU
# assistant   = 253,-1       # pesan AI DENZYX
# yellow      = 220,-1       # model list / reasoning / output tool
# error       = 196,-1       # pesan error
# label_user  = 231,24      # label ── KAMU ── (teks, latar)
# label_ai    = 231,27      # label ── AI DENZYX ──
# label_tool  = 231,30      # label ── TOOL ──
# label_error = 231,196     # label ── ERROR ──
# md_heading  = 39,-1       # judul markdown (## dst)
# md_code     = 220,-1      # kode markdown (`kode`, blok ```)
# md_bullet   = 46,-1       # bullet/daftar markdown
# md_quote    = 39,-1       # kutipan markdown (>)
